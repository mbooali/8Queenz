#include <iostream>
#include "time.h"
#include "Gen.h"

using namespace std;

int main()
{
	srand(time(0));
/*	Gen a,b,c;
	for(int i = 0; i < 8; i++ )
	{
		a.num[i] = i + 1;
		b.num[i] = 8 - i;
	}
	cin >> a ;
	a<b;
	cout << "\n" << a[2] << "\n" << b << endl;
*/
	return 0;
}

